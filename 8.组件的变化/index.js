/* 
           
    路由 vue-router@next

        改为全是具名导出
           createRouter        // 创建一个路由应用
           createWebHistory    // 创建浏览器h5路由跳转模式
           createWebHashHistory  // 创建浏览器hash路由跳转模式
           createMemoryHistory   // 创建内存路由跳转模式
           createRouterMatcher 
           useRouter            // 获取路由实例

        
        createRouter 配置对象参数
           history: createWebHistory(),   // 以前为 mode: 'history'


    
     

        vue3内置组件
           teleport    该组件会将插槽内容放到指定标签里的末尾。但是插槽内容仍然在对应的组件内进行控制
             to属性： css选择器。  会将插槽内容放到给选择器选中的标签里的末尾


             teleport  吐音 T哩凤特
           


*/